@import "tailwindcss";

/* ═══════════════════════════════════════════════
   DESIGN TOKENS — Linear-inspired System
   All colors, spacing, typography centralized.
   ═══════════════════════════════════════════ */

:root {
  /* ── Colors: Night Ocean (Default Dark Theme) ── */
  --color-bg-app: #07111d;
  --color-bg-sidebar: #0b1624;
  --color-bg-panel: #0f1b2b;
  --color-bg-surface: #132235;
  --color-bg-hover: #1b2e47;
  --color-bg-elevated: #172b42;

  /* ── Colors: Borders & Dividers ── */
  --color-border: #284057;
  --color-row-border: rgba(40, 64, 87, 0.5);
  --color-border-strong: #3b5a7a;

  /* ── Colors: Text ── */
  --color-text-primary: #f8fafc;
  --color-text-secondary: #cbd5e1;
  --color-text-tertiary: #94a3b8;
  --color-text-muted: #7f95aa;
  --color-text-on-accent: #ffffff;
  --color-overlay: rgba(7, 17, 29, 0.86);

  /* ── Colors: Semantic ── */
  --color-accent: #0ea5e9;
  --color-accent-hover: #38bdf8;
  --color-accent-subtle: rgba(14, 165, 233, 0.14);
  --color-success: #10b981;
  --color-success-subtle: rgba(16, 185, 129, 0.14);
  --color-warning: #f59e0b;
  --color-warning-subtle: rgba(245, 158, 11, 0.14);
  --color-danger: #ef4444;
  --color-danger-subtle: rgba(239, 68, 68, 0.14);
  --color-note-bg: rgba(245, 158, 11, 0.14);
  --color-note-border: rgba(245, 158, 11, 0.28);
  --color-note-text: #fbbf24;

  /* ── Typography ── */
  --font-family: 'Inter', system-ui, -apple-system, sans-serif;
  --font-code: 'Fira Code', monospace;
  --font-size-xs: 11px;
  --font-size-sm: 12px;
  --font-size-base: 13px;
  --font-size-md: 14px;
  --font-size-lg: 16px;
  --line-height-tight: 1.35;
  --line-height-normal: 1.45;
  --line-height-relaxed: 1.55;

  /* ── Spacing (4px base) ── */
  --space-0: 0;
  --space-1: 4px;
  --space-2: 8px;
  --space-3: 12px;
  --space-4: 16px;
  --space-5: 20px;
  --space-6: 24px;
  --space-8: 32px;

  /* ── Border Radius ── */
  --radius-sm: 6px;
  --radius-md: 8px;
  --radius-lg: 10px;
  --radius-xl: 12px;
  --radius-full: 9999px;

  /* ── Shadows ── */
  --shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.18);
  --shadow-md: 0 8px 24px rgba(0, 0, 0, 0.26);

  /* ── Transitions ── */
  --transition-fast: 140ms ease-in-out;
  --transition-normal: 180ms ease-in-out;

  /* ── Z-Index Scale ── */
  --z-dropdown: 10;
  --z-sticky: 20;
  --z-overlay: 30;
  --z-modal: 40;
  --z-tooltip: 50;

  /* ── Sizes ── */
  --sidebar-width: 48px;
  --conversation-width: 320px;
  --lead-panel-width: 300px;
  --header-height: 64px;
  --btn-height-sm: 28px;
  --btn-height-md: 32px;
  --btn-height-lg: 36px;
  --input-height-sm: 32px;
  --input-height-md: 36px;
  --input-height-lg: 40px;
  --icon-size: 16px;
  --icon-stroke: 1.75;
}

/* ── Colors: Day Rose (Light Theme) ── */
.light-theme {
  --color-bg-app: #f6f7f9;
  --color-bg-sidebar: #ffffff;
  --color-bg-panel: #ffffff;
  --color-bg-surface: #f1f3f6;
  --color-bg-hover: #eef4ff;
  --color-bg-elevated: #ffffff;

  --color-border: #e5e7eb;
  --color-row-border: #f3f4f6;
  --color-border-strong: #d1d5db;

  --color-text-primary: #111827;
  --color-text-secondary: #4b5563;
  --color-text-tertiary: #6b7280;
  --color-text-muted: #9ca3af;
  --color-text-on-accent: #ffffff;
  --color-overlay: rgba(17, 24, 39, 0.62);

  --color-accent: #2684ff;
  --color-accent-hover: #0f6eea;
  --color-accent-subtle: #eaf3ff;
  --color-success: #00a86b;
  --color-success-subtle: #e9fbf2;
  --color-warning: #ff6b2c;
  --color-warning-subtle: #fff1e8;
  --color-danger: #ef4444;
  --color-danger-subtle: #feecec;
  --color-note-bg: #fff8db;
  --color-note-border: #f5d667;
  --color-note-text: #92400e;

  --shadow-sm: 0 1px 2px rgba(15, 23, 42, 0.06);
  --shadow-md: 0 12px 30px rgba(15, 23, 42, 0.10);
}

/* ── Base Reset ── */
* { box-sizing: border-box; margin: 0; padding: 0; }

html, body, #root {
  width: 100%; height: 100dvh;
  overflow: hidden;
  background: var(--color-bg-app);
  color: var(--color-text-primary);
  font-family: var(--font-family);
  font-size: var(--font-size-base);
  line-height: var(--line-height-normal);
  -webkit-font-smoothing: antialiased;
}

/* ── Reduced Motion ── */
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}

/* ═══════════════════════════════════════════════
   LAYOUT SYSTEM — Desktop (≥1440px)
   ═══════════════════════════════════════════════ */

.app-grid {
  display: grid;
  grid-template-columns: var(--sidebar-width) var(--conversation-width) 1fr var(--lead-panel-width);
  height: 100dvh;
  overflow: hidden;
}

.app-grid {
  display: grid;
  grid-template-columns: var(--sidebar-width) var(--conversation-width) 1fr var(--lead-panel-width);
  height: 100dvh;
  max-height: 100dvh;
  overflow: hidden;
}

.app-grid-collapsed {
  display: grid;
  grid-template-columns: var(--sidebar-width) var(--conversation-width) 1fr;
  height: 100dvh;
  max-height: 100dvh;
  overflow: hidden;
}

.app-grid > *,
.app-grid-collapsed > * {
  min-width: 0;
  min-height: 0;
  height: 100%;
  max-height: 100dvh;
}

/* ═══════════════════════════════════════════════
   ≤ 1439px: Narrow panels
   ═══════════════════════════════════════════════ */
@media (max-width: 1439px) {
  .app-grid {
    grid-template-columns: 48px 320px 1fr 300px;
  }
  .app-grid-collapsed {
    grid-template-columns: 48px 320px 1fr;
  }
}

/* ═══════════════════════════════════════════════
   ≤ 1199px: Lead panel as drawer
   ═══════════════════════════════════════════════ */
@media (max-width: 1199px) {
  .app-grid {
    grid-template-columns: 48px 320px 1fr;
  }
  .app-grid-collapsed {
    grid-template-columns: 48px 320px 1fr;
  }

  .lead-panel-hidden {
    display: none;
  }

  .lead-drawer {
    position: fixed;
    top: 0;
    right: 0;
    bottom: 0;
    width: var(--lead-panel-width);
    max-width: 90vw;
    z-index: var(--z-overlay);
    transform: translateX(100%);
    transition: transform var(--transition-normal);
  }

  .lead-drawer.open {
    transform: translateX(0);
  }

  .lead-drawer-backdrop {
    position: fixed;
    inset: 0;
    background: rgba(0, 0, 0, 0.5);
    z-index: calc(var(--z-overlay) - 1);
    opacity: 0;
    pointer-events: none;
    transition: opacity var(--transition-fast);
  }

  .lead-drawer-backdrop.open {
    opacity: 1;
    pointer-events: auto;
  }
}

/* ═══════════════════════════════════════════════
   ≤ 899px: Master-detail for conversation/chat
   ═══════════════════════════════════════════════ */
@media (max-width: 899px) {
  .app-grid {
    grid-template-columns: 48px 1fr;
  }
  .app-grid-collapsed {
    grid-template-columns: 48px 1fr;
  }

  .conversation-full-screen {
    grid-column: 2 !important;
    width: 100%;
  }

  .chat-overlay {
    position: absolute;
    inset: 0;
    z-index: var(--z-dropdown);
    display: none;
  }

  .chat-overlay.active {
    display: flex;
  }

  .back-button {
    display: inline-flex !important;
  }
}

@media (min-width: 900px) {
  .back-button {
    display: none !important;
  }
}

/* Chat Area */
.chat-area {
  display: flex;
  flex-direction: column;
  height: 100%;
  min-height: 0;
  max-height: 100dvh;
  position: relative;
  overflow: hidden;
  background: var(--color-bg-app);
}

.chat-thread {
  display: flex;
  flex-direction: column;
  height: 100%;
  min-height: 0;
  overflow: hidden;
}

.chat-messages {
  flex: 1 1 0;
  min-height: 0;
  overflow-y: auto;
  overflow-x: hidden;
}

.chat-content-wrapper {
  max-width: 820px;
  min-height: 100%;
  margin: 0 auto;
  width: 100%;
  padding: 0 var(--space-4);
}

.chat-composer {
  flex: 0 0 auto;
  flex-shrink: 0;
  position: relative;
  z-index: var(--z-sticky);
  min-height: 72px;
  border-top: 1px solid var(--color-border);
  background: var(--color-bg-panel);
}

/* Message Layout */
.message-row {
  display: flex;
  gap: var(--space-2);
}

.message-row + .message-row { margin-top: 0; }

.message-group-gap { margin-bottom: var(--space-4); }

.message-bubble {
  max-width: min(560px, 100%);
  min-width: 60px;
  width: fit-content;
  padding: var(--space-2) var(--space-3);
  border-radius: var(--radius-xl);
  font-size: var(--font-size-base);
  line-height: var(--line-height-normal);
  white-space: pre-wrap;
  overflow-wrap: break-word;
  word-break: break-word;
}

.message-bubble.incoming { border-bottom-left-radius: var(--radius-sm); }
.message-bubble.outgoing { border-bottom-right-radius: var(--radius-sm); }

.message-time { font-size: var(--font-size-xs); color: var(--color-text-tertiary); margin-top: 2px; }

/* Scrollbar */
::-webkit-scrollbar { width: 6px; height: 6px; }
::-webkit-scrollbar-track { background: transparent; }
::-webkit-scrollbar-thumb { background: rgba(255, 255, 255, 0.18); border-radius: var(--radius-sm); }
::-webkit-scrollbar-thumb:hover { background: rgba(255, 255, 255, 0.35); }
html.light-theme ::-webkit-scrollbar-thumb { background: rgba(148, 163, 184, 0.45); }
html.light-theme ::-webkit-scrollbar-thumb:hover { background: rgba(100, 116, 139, 0.60); }

/* Select & Option Dropdown Fix */
select {
  background-color: var(--color-bg-surface);
  color: var(--color-text-primary);
}

option {
  background-color: var(--color-bg-panel);
  color: var(--color-text-primary);
  padding: 6px 12px;
}

/* Utility */
.linear-transition { transition: all var(--transition-fast) ease-in-out; }
.tabular-nums { font-variant-numeric: tabular-nums; }
